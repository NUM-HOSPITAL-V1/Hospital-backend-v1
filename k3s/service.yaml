apiVersion: v1
kind: Service
metadata:
  name: hospital-backend
  namespace: num
spec:
  type: ClusterIP
  selector:
    app: hospital-backend
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
